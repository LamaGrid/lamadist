# Headless auto-target manifest (kas/extras/headless-auto.kas.yml
# bakes this onto the stick as manifest.env).  Installs to the sole
# eligible internal disk with zero prompts; aborts when the machine
# has more or fewer than exactly one.
MANIFEST_VERSION=1
TARGET_DISK=auto
CONFIRM=auto
HEADLESS=yes
