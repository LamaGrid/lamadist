# lamadist-etc-overlay.service (lamadist-image.bbclass) mounts a
# writable overlay on /etc at every boot.  Ensure the kernel
# supports it explicitly rather than relying on the base kernel
# type's default, mirroring erofs.cfg's precedent.
CONFIG_OVERLAY_FS=y
