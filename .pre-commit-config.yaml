# SPDX-License-Identifier: Apache-2.0
repos:
  - repo: local
    hooks:
      - id: check-scans
        name: Check security scans
        entry: .mise/hooks/check-scans.sh
        language: script
        stages: [pre-push]
        always_run: true
        pass_filenames: false
